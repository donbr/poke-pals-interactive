# OpenAI API Configuration
# Required for all AI features (Professor Pine chat, story generation, quizzes, etc.)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Development Instructions:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Replace the placeholder with your actual OpenAI API key
# 3. Never commit .env.local to version control (it's in .gitignore)

# Production Deployment (Vercel):
# Set OPENAI_API_KEY in: Project Settings → Environment Variables
# https://vercel.com/docs/environment-variables
